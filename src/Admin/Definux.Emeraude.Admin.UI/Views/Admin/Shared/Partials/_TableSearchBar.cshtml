<form method="get" class="mb-4" action="@Url.GetDefaultUrl(ViewData)">
    <div class="d-flex m-0">
        <input type="text" name="searchQuery" class="form-control" value="@ViewData.GetSearchQuery()" placeholder="Search..">
        <button class="btn btn-primary ml-3" type="submit">Search</button>
    </div>
</form>